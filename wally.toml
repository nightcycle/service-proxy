[package]
name = "nightcycle/service-proxy"
description = "A wally package that creates an object that points towards a constructed service"
version = "1.0.6"
license = "Apache"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
include = ["src", "default.project.json", "LICENSE"]

[dependencies]
